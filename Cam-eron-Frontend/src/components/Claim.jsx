export default function Claim({ newCaseProp }) {
    return (
        <div>
            <div>Hello from the claim team</div>
            <button id="back" onClick={newCaseProp}>Back</button>
        </div>
    )
}