export default function EditClaim({ claimID, userAnswers, setEditClaim }) {
    return (
        <div className="flex flex-row">
            <p>Hello there!</p>
            <button onClick={setEditClaim}>Back</button>
        </div>
    )
}